This directory exists solely so this repo always has recent commit
activity, which stops GitHub from auto-disabling the scheduled
workflows (mvs-scan.yml, mvs-commands.yml, mvs-weekly.yml) after 60
days of repo inactivity. heartbeat.txt is updated daily by
keepalive.yml. Safe to ignore -- nothing in here affects the bot's
trading logic.
