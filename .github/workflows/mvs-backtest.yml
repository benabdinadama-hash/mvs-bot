name: MVS Backtest
on:
  workflow_dispatch:
    inputs:
      symbol:
        description: 'Symbol(s), comma-separated (leave blank for all pairs in config.js SYMBOLS)'
        required: false
      days:
        description: 'Days to backtest (leave blank for config.BACKTEST_DAYS)'
        required: false
      # v10.23: removed the `wick` (NEAR_ZONE_USE_WICK) toggle that used
      # to live here. That setting is now a permanent default in
      # config.js itself (see that file) — one place to change it, not
      # two, and no more remembering to check a box each run.

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - run: npm install
      - run: node backtest.js ${{ inputs.symbol }} ${{ inputs.days }}
      - name: Save backtest report
        run: |
          mkdir -p /tmp/reports
          cp backtest-report.txt /tmp/reports/ 2>/dev/null || true
          cp backtest-report.json /tmp/reports/ 2>/dev/null || true
      - uses: actions/upload-artifact@v5
        with:
          name: backtest-report
          path: /tmp/reports/
