# Secrets — never commit these (explicit names, NOT a wildcard, so the
# .example template files below stay trackable and visible in the repo)
.env
.env.sh
.env.local
*.pem
*.key

# Dependencies
node_modules/

# OS junk
.DS_Store
execution/keys.sh
execution/executed-signals.json
execution/logs/
execution/heartbeat.json
